/**
 * Enum for workspace modes to provide type safety
 * instead of using string literals throughout the codebase
 */
export enum WorkspaceMode {
  CHAPTER = 'chapter',
  CROSS_REFERENCES = 'crossReferences',
  TOPICAL = 'topical'
}